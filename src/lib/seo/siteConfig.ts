// Single source of truth for the production origin — sitemap.ts, robots.ts,
// and per-page canonical/OG URLs all key off this instead of each hardcoding
// their own copy of "https://colevitate.com".
export const SITE_URL = "https://colevitate.com";
